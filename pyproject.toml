[project]
name = "security-agent"
version = "0.2.0"
description = "Solidity smart contract security auditing agent with intelligent context analysis"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "litellm>=1.0.0",
    "python-dotenv>=1.0.0",
]
